'use client'

import { PaymentBoard } from '@/components/payments/board/PaymentBoard'

/* Sayfa ince bir kabuk — tüm mantık ve dört görünüm alternatifi
   PaymentBoard içinde (bkz. board-pages-pattern kalıbı). */
export default function PaymentsPage() {
  return <PaymentBoard />
}
