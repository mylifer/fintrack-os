'use client'

import { InvestmentBoard } from '@/components/investments/board/InvestmentBoard'

/* Sayfa ince bir kabuk — tüm mantık ve dört görünüm alternatifi
   InvestmentBoard içinde (bkz. board-pages-pattern kalıbı). */
export default function InvestmentsPage() {
  return <InvestmentBoard />
}
