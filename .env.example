# FinTrack OS — ortam değişkenleri
# Bu dosyayı .env.local olarak kopyalayıp değerleri doldurun (.env* git'e girmez).

# Supabase projesi → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...

# YALNIZ yerel geliştirme / uçtan uca testler: 1 iken giriş yapmadan uygulama
# açılır (veri yalnız tarayıcıdaki IndexedDB'de kalır, buluta yazılmaz).
# NODE_ENV=production'da yok sayılır; Vercel'e EKLEMEYİN.
# AUTH_BYPASS=1
